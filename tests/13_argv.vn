func main(args: list<string>) -> i64 {
  print("# of args: ");
  printint(length(args));
  let i: i64 = 0;
  while i < length(args) {
    print("Arg: ");
    println(args[i]);
    i = i + 1;
  }
  return 0;
}
